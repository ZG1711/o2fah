export class AppSettings {
  biometricEnabled: boolean = false;
  autoLockTimeout: number = 0;
  backupReminder: string = 'never';
  themeMode: string = 'system';
  defaultSort: string = 'manual';
  lastBackupTime: number = 0;
}
